import React from 'react';

const Loader = () => {
    return (
        <div className="pageloader is-active is-dark is-right-to-left">
            <span className="title">Thanks for rocking with the Nation!</span>
        </div>
    )
};

export default Loader;